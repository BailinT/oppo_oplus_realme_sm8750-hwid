# 当前会话交接

## 最新状态（2026-07-23）
- 已从 `BailinT/oppo_oplus_realme_sm8750` 复制出独立副本，并创建公开新仓库：`https://github.com/BailinT/oppo_oplus_realme_sm8750-hwid`。
- 新仓库是独立仓库，不是 GitHub fork；`origin` 指向新仓库，原仓库作为 `upstream` 只读来源且 push URL 为 `DISABLED`。
- 已实现全白名单模式：`hwid/allowlist.txt` 中的所有 HWID 都编译进同一个内核；名单外或读取不到 HWID 的设备在 Android 用户空间启动前 panic，`panic_timeout=1` 后重启。
- 当前白名单只有 `abbe0d2c`。
- 已接入所有 `fastbuild_6.6.*.yml` 编译 workflow 和所有 `local/builder_*.sh` 本地编译入口。
- GitHub 已识别全部 10 个 workflow，其中 7 个内核构建 workflow 均为 `active`；Actions 权限已启用。
- 功能提交：`f0026e9b13e4bce25e53fea455b3c9ce3`。
- 已推送新仓库；未修改原仓库，未自动触发或 cancel 任何 workflow。

## 已改文件
- `hwid/allowlist.txt`
- `hwid/hwid_lock.c`
- `hwid/inject_hwid_lock.sh`
- `hwid/README.md`
- `.github/workflows/fastbuild_6.6.*.yml`
- `local/builder_*.sh`
- `AGENTS.md`

## 实现要点
- 优先从 Android bootconfig 读取 `androidboot.chipid`，兼容读取 `oplusboot.serialno`。
- 同时兼容 legacy kernel command line。
- `inject_hwid_lock.sh` 会校验并去重白名单，然后生成内核源码内的 `init/hwid_allowlist.h`，把 `hwid_lock.o` 编入 `init`，并在 `setup_command_line()` 后执行校验。
- 不要把 `hwid` 作为 workflow 输入；用户需求是同一个镜像允许整个白名单，而不是一次构建绑定单个 ID。

## 已执行命令与验证
- 已读取原 HWID 查询脚本，确认欧加系扫描顺序是 `androidboot.chipid oplusboot.serialno`。
- 已读取 6.6.89 workflow、本地 builder、上游 `init/main.c`、`init/Makefile`、`include/linux/bootconfig.h`。
- 已执行 `git diff --check`，未发现 whitespace error。
- 已执行 workflow YAML 的基础 tab 检查；未发现 YAML tab。
- Linux `bash -n` / 完整注入模拟受当前 Windows 环境限制，`bash` 不是 Git Bash，Python 运行时也没有 PyYAML；尚未完成真实内核编译。

## 产物
- 副本目录：`E:\DeepSeek-Reasonix\GKI\oppo_oplus_realme_sm8750-hwid`
- 当前无构建产物、无 hash、无远程发布。

## 下一步
1. 在新仓库 GitHub Actions 上先用只含 `abbe0d2c` 的白名单构建一次。
2. 刷入前保存原 boot/vendor_boot 镜像和可用 fastboot/recovery 回滚路径。
3. 确认本机脚本显示的 HWID 与 bootconfig/cmdline 值一致后再刷入。
4. 需要放行其他设备时，在 `hwid/allowlist.txt` 新增一行并重新构建。

## 注意事项
- 同一套 HWID 校验已在 `BailinT/oppo_oplus_realme_sm8650-hwid` 真机测试成功；sm8750 仍需单独构建和刷入验证。
- 这是启动阻断机制，不是安全启动签名；具备修改内核镜像能力的人仍可能移除校验。
- 如果厂商链路在更早阶段覆盖或隐藏 bootconfig，需从实际设备 `cat /proc/bootconfig` 与 `cat /proc/cmdline` 复核字段来源。
- 错误白名单或错误字段会导致循环重启，必须保留原始 boot 镜像。
